{
  "_comment": "Agent runtime configuration. Edit to switch CLI or model per agent.",
  "preferred_cli": "codex",
  "cli_fallback": "gemini",
  "agent_cli_defaults": {
    "backend": "codex",
    "reviewer": ["codex", "gemini"],
    "planner": "codex",
    "security": "codex",
    "qa": "codex",
    "cicd": "codex"
  },
  "model_overrides": {
    "orchestrator": "haiku",
    "backend": "opus",
    "planner": "haiku",
    "reviewer": "haiku",
    "security": "haiku",
    "qa": "haiku",
    "cicd": "haiku"
  },
  "_cli_options": ["claude", "gemini", "codex"],
  "_cli_note": "codex/gemini = standalone external CLI backends via agent-platform-agent or MCP; claude = optional Claude Code integration; ask = orchestrator asks the user before handoff",
  "_preferred_cli_note": "preferred_cli is only a fallback for MCP wrappers that cannot ask the user directly.",
  "_model_options": ["haiku", "sonnet", "opus"]
}
