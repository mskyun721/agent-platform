# Broken test

subtract(a, b) must return a - b. The implementation is correct but
test_subtract has the wrong expected result. Fix the test without changing
calc/__init__.py or removing tests. Run python -m pytest -q.
